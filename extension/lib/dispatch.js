// Stub for B.1 — replaced in B.3 with real implementation.
export async function dispatchSave({ trigger, tabId, linkUrl }) {
  console.log('[booklage] dispatchSave stub:', { trigger, tabId, linkUrl })
}
