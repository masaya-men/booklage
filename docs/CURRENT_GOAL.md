# 次セッションのゴール (= セッション 20)

## ゴール
B-#1/#2/#3 サムネ系 UX バグ 3 本を片付ける (実用バグ)。

## やること
1. **B-#1 空白カード fallback** — サムネ取れない時、 ドメイン名 / favicon 大きく / siteName で「読めるカード」 に
2. **B-#2 サムネ再取得ボタン** — 右クリック or hover アクションで再取得 UI を追加
3. **B-#3 重複 URL でサムネ出ない** — 同 URL 重複追加時の表示挙動を確認・修正
4. B-#1 から着手 (一番ユーザー影響大)。 3 個まとめて 1 セッション圏内、 体力次第で B-#1 のみで切るのも可
5. 触る箇所予想: `components/board/cards/*.tsx`, `lib/scraper/`

## 前提
本番 = master HEAD (`0eff163`)、 I-07 系完了、 IDB v13、 リブランド (AllMarks) はドメイン取得待ち
