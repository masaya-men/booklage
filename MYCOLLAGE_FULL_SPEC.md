# Booklage — 完全要件定義書（最終承認版）

> **このファイルは「記憶を失っても困らない」プロジェクト設計書です。**
> 全ての決定事項、技術仕様、UIデザイン、運用方針、ロードマップを含みます。
> Claude Code は必ずこのファイルと `CLAUDE.md` の両方を読んでから作業を開始すること。

---

## ✅ 決定事項: プロジェクト名 & ドメイン

| 項目 | 決定 |
|------|------|
| **プロジェクト名** | **Booklage**（ブックラージュ） |
| **由来** | book(mark) + (col)lage |
| **ドメイン** | booklage.com（要取得確認） |
| **タグライン** | Bookmark × Collage |
| **決定日** | 2026-04-10 |

### 命名の経緯

以下の候補は既存サービスとの衝突により却下:
- Pikboard → pikboard.com がフランスの顧客管理ソフトとして稼働中
- Markage → カナダのマーケティング会社 + iOSアプリ「Markage」が存在
- Clippage → clippage.app で「ClipPage」というWebツールが稼働中

Booklageは8文字、既存サービスとの衝突ゼロ、「ブックマーク×コラージュ」が直感的に伝わる。
ロゴデザインで「Bookmark × Collage」のコンセプトを視覚的に補完する方針。

---

## 1. プロジェクト概要

### コンセプト
あらゆるWebサイト（Twitter/X, YouTube, TikTok, Instagram, 一般Webサイト等）のブックマークを、
**服飾学生のコラージュ / ムードボード**のような美しいビジュアルボードとして管理・フォルダ分け・共有できるWebアプリ。

### ミッション
「整理ツール」ではなく **「表現ツール」**。
コラージュを画像にしてSNSでシェアさせるバイラルループで成長する。
超モダンで目が楽しい、アニメーションとインタラクションが命のアプリ。

### ターゲット
- **世界中の全ユーザー**（ターゲットは絞らない）
- 特にブックマーク機能が弱いプラットフォーム（X, YouTube等）のユーザー
- 多言語対応で初日から世界展開

### 設計の前提
- 全 URL に対応する汎用ブックマークツールとして設計
- ブクマを「収集」だけでなく「ビジュアル表現」できる体験を提供
- ユーザーデータは端末内のみで完結する Privacy First 設計

---

## 2. 承認済み決定事項

| 項目 | 決定 | 備考 |
|------|------|------|
| フレームワーク | **Next.js 14+** (App Router, TypeScript) | インタラクション豊富なアプリに最適 |
| スタイリング | **Vanilla CSS + CSS Variables** | 独自コラージュ感を出すため |
| ホスティング | **Cloudflare Pages** | 帯域無制限・完全¥0 |
| データ保持 | **ブラウザ内のみ（IndexedDB）** | サーバーにデータ送らない |
| ブックマーク方式 | **ブックマークレット**（ページ上にフォルダ選択ポップアップ表示） | 拡張機能は将来検討 |
| 対応プラットフォーム | **全URL** | OGP/oEmbed で自動取得 |
| ターゲット | **世界中（絞らない）** | |
| 多言語 | **15言語（段階的）** | 日英→中韓→欧州→他 |
| UIコンセプト | **服飾学生のコラージュ** | Glassmorphism 2.0 + 最新トレンド |
| チーム | **1人 + Claude Code** | |
| 費用方針 | **完全¥0運営** | 初期¥2,500（ドメインのみ）、月額¥0 |

---

## 3. 技術スタック

| 役割 | 選定 | 備考 |
|------|------|------|
| **Framework** | Next.js 14+ (App Router) | TypeScript strict mode |
| **Styling** | Vanilla CSS + CSS Custom Properties | Tailwind不使用 |
| **Tweet表示** | react-tweet | widgets.js 禁止（CORS・GDPR問題） |
| **動画埋め込み** | oEmbed統一プロトコル | YouTube, TikTok等 |
| **URL情報取得** | ブックマークレット（主導線）/ Worker（任意） | ブックマークレットはWorker不要でOGP取得 |
| **テキスト計測** | @chenglou/pretext | カード内テキスト精密レイアウト |
| **ドラッグ** | GSAP Draggable or vanilla JS | Framer Motion 禁止 |
| **ローカルDB** | IndexedDB via `idb` | ブラウザ内保存 |
| **共有** | URL圧縮エンコード方式 | サーバー不要・URLにコラージュデータを埋め込み |
| **画像出力** | dom-to-image-more | コラージュPNG書き出し |
| **多言語** | next-intl or i18next | 15言語対応 |
| **Hosting** | Cloudflare Pages | 帯域無制限・世界310拠点 |
| **Analytics** | Cloudflare Analytics | Cookie不要・無料 |
| **動画制作** | Remotion | プロモ動画をコードで生成 |

---

## 4. アーキテクチャ

```
┌──────────────────────────────────────────────────┐
│         Cloudflare Pages（世界310拠点）             │
│         帯域幅無制限 → 何億PVでもサイトが落ちない     │
│         月額¥0（無料プランのみ）                     │
│                                                    │
│  ┌──────────────────────────────────────────────┐  │
│  │   Pages（静的サイト配信）                       │  │
│  │   静的HTML/CSS/JS を世界中に高速配信            │  │
│  │   Workers/R2/KV は一切使わない                  │  │
│  └──────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
                        │
                        ↓ 世界中のユーザー
┌──────────────────────────────────────────────────┐
│              ユーザーのブラウザ（全処理はここ）       │
│  ┌──────────────────────────────────────────────┐  │
│  │ IndexedDB → ブックマーク・フォルダ・配置を保存   │  │
│  │ → サーバーにデータ送らない（プライバシー安全）    │  │
│  │ → オフラインでも閲覧可能（PWA）                 │  │
│  └──────────────────────────────────────────────┘  │
│  ブックマークレット → OGP情報を直接取得（Worker不要）│
│  共有 → URL圧縮エンコード（サーバー保存不要）       │
│  アニメーション・ドラッグ・コラージュ描画             │
│  → 全てブラウザ内で処理（サーバー負荷ゼロ）          │
└──────────────────────────────────────────────────┘
```

### コスト

| 月間PV | 推定月額 |
|--------|---------|
| 何億PVでも | **¥0**（Cloudflare Pages無料枠のみ） |

Workers/R2/KVを使わないため、PV数に関係なく月額¥0。
Cloudflare Pagesの無料枠: ビルド500回/月、帯域無制限。

---

## 5. ユニバーサルブックマーク設計

### URL情報取得フロー

```
【主導線】ブックマークレットで保存
    ↓ ページ上のOGPメタタグを直接読み取り（Worker不要）
    ↓ タイトル・説明文・サムネイル・ファビコンを取得
    ↓ IndexedDB に保存 → カード生成 → コラージュに配置

【フォールバック】URLペーストで保存
    ↓
URLパターン判定
    ├── x.com/twitter.com → react-tweet でツイート表示
    ├── youtube.com        → oEmbed → 動画プレーヤー埋め込み
    ├── tiktok.com         → oEmbed → 動画プレーヤー埋め込み
    ├── instagram.com      → oEmbed → 投稿埋め込み
    └── その他全URL        → URLのみでカード作成（OGP情報なし）
                             ※ Worker（OGPスクレイパー）があればOGP取得可能だが必須ではない
```

全て無料（APIキー不要）。ブックマークレットが主導線のためWorker不要で動作する。

### フォルダ構造

```
ユーザーのコラージュ
├── 📁 ファッション（カスタムフォルダ）
│   ├── 🐦 @stylist のツイート
│   ├── 🎥 TikTok 動画
│   └── 🌐 ZARA の新作ページ
├── 📁 テック（カスタムフォルダ）
│   ├── 📺 YouTube チュートリアル
│   └── 🌐 tech blog 記事
└── 📁 推し活（カスタムフォルダ）
    ├── 🐦 推しのツイート
    └── 📸 Instagram の投稿
```

各フォルダ = 1つのコラージュキャンバス。

### ブックマークレット

ページ上にフォルダ選択ポップアップを表示する方式:

```
ユーザーがブックマークレットをクリック
    ↓
現在のページ上に小さなポップアップ
    ┌───────────────────────┐
    │  Booklage に保存      │
    │  📁 ファッション       │
    │  📁 テック             │
    │  📁 料理               │
    │  + 新しいフォルダ       │
    │     [保存✨]            │
    └───────────────────────┘
    ↓
保存完了アニメーション → ポップアップ消える
ユーザーはそのページに留まったまま
```

---

## 6. UIデザイン

### デザインコンセプト

```
超モダン × 服飾コラージュ × 目が楽しい
```

### 2026年デザイントレンド採用

| トレンド | 適用方法 |
|----------|---------|
| **Glassmorphism 2.0** | メニュー・ナビゲーションに半透明ガラス効果。カードも設定で切替可能 |
| **マイクロインタラクション** | 全ボタン・カードにホバー・クリック時の微細アニメーション |
| **3D / 空間的奥行き** | カードの影・角度・重なりで立体感 |
| **ベントグリッド** | LP・メニューに整理された格子レイアウト |
| **大胆なタイポグラフィ** | LPキャッチコピーにモダンフォント（Google Fonts: Inter, Outfit等） |
| **ダークモード標準** | デフォルトはダーク。明るいテーマも選択可 |

### アニメーション一覧（8種）

| 演出 | 説明 | 実装方法 |
|------|------|---------|
| **ふわふわ浮遊** | 全カードが常に微かにゆらゆら揺れる | CSS @keyframes + animation-delay ランダム |
| **フォルダ切り替え** | カードが四方八方に散り、新しいカードが飛んでくる | GSAP Timeline |
| **ランダムピック** | スロットマシンのようにカードが回転して1枚選出 | GSAP + CSS transforms |
| **ドラッグ移動** | 掴むと影が深くなり、離すとふわっと着地 | GSAP Draggable |
| **ホバー** | 触れると拡大 + グロー効果 | CSS :hover + transform |
| **新規保存** | 上から降ってきてバウンド着地 | GSAP with bounce ease |
| **削除** | くるくる回りながらフェードアウト | GSAP rotate + opacity |
| **色別サジェスト** | 他の色が透明化し、選択色だけ強調 | CSS opacity transition |

### 背景テーマ（9種類・選択式）

| テーマ | CSS実装方針 |
|--------|-----------|
| ミニマルホワイト | `background: #fafafa` |
| 方眼紙 | `background-image: linear-gradient(grid pattern)` |
| コルクボード | テクスチャ画像 |
| ダークモード | `background: #0a0a0a` + カード発光 |
| 宇宙 | グラデーション + 星パーティクル |
| 自然（森） | テクスチャ画像 + 緑のオーバーレイ |
| 海 | 波グラデーション |
| カスタムカラー | カラーピッカー |
| カスタム画像 | ユーザーアップロード → IndexedDB保存 |

### デザインインスピレーション参照先
- [Awwwards](https://www.awwwards.com/) — 最先端Webデザイン受賞作
- [Mobbin](https://mobbin.com/) — 一流アプリUIパターン
- [Muzli](https://muz.li/) — リアルタイムデザイントレンド

---

## 7. 多言語対応（15言語）

| 優先度 | 言語 | タイミング |
|--------|------|-----------|
| **P0** | 🇯🇵 日本語、🇬🇧 英語 | 初日 |
| **P1** | 🇨🇳 中国語（簡体）、🇰🇷 韓国語 | 1週後 |
| **P2** | 🇪🇸 スペイン語、🇫🇷 フランス語、🇩🇪 ドイツ語、🇵🇹 ポルトガル語、🇮🇹 イタリア語 | 2週後 |
| **P3** | 🇳🇱 オランダ語、🇹🇷 トルコ語、🇷🇺 ロシア語、🇸🇦 アラビア語（RTL）、🇹🇭 タイ語、🇻🇳 ベトナム語 | 1ヶ月後 |

- UIテキスト量は約500〜1,000ワード（非常に少ない）
- 翻訳はClaude/AIで一括生成（¥0）
- ネイティブチェックは将来予算に応じて実施（全15言語で¥11,250〜¥56,250の1回費用）
- ブラウザの言語設定を検出して自動切り替え + 手動切り替えUI

### 翻訳ファイル構成

```
messages/
├── ja.json     # 日本語（デフォルト）
├── en.json     # 英語
├── zh-CN.json  # 中国語（簡体）
├── ko.json     # 韓国語
├── es.json     # スペイン語
├── fr.json     # フランス語
├── de.json     # ドイツ語
├── pt.json     # ポルトガル語
├── it.json     # イタリア語
├── nl.json     # オランダ語
├── tr.json     # トルコ語
├── ru.json     # ロシア語
├── ar.json     # アラビア語
├── th.json     # タイ語
└── vi.json     # ベトナム語
```

---

## 8. データ保持方針: 「持たない経営」

- **ユーザー登録なし**
- **データはブラウザ内（IndexedDB）にのみ保存**
- **サーバーにユーザーデータを送らない**
- **GDPR/個人情報保護法の対応がほぼ不要**
- **共有**: URL圧縮エンコード方式（サーバー保存なし。コラージュデータをURLパラメータに圧縮して埋め込み）
- **個人情報は一切含まない**（コラージュ配置情報とURL一覧のみ）

### IndexedDB スキーマ

```typescript
interface AppDB {
  bookmarks: {
    key: string              // UUID
    value: {
      url: string            // 保存したURL
      title: string          // ページタイトル
      description: string    // 説明文
      thumbnail: string      // サムネイル画像URL
      favicon: string        // ファビコンURL
      siteName: string       // サイト名
      type: 'tweet' | 'youtube' | 'tiktok' | 'instagram' | 'website'
      savedAt: Date
      folderId: string
    }
    indexes: { 'by-folder': string; 'by-date': Date }
  }
  folders: {
    key: string              // UUID
    value: {
      name: string           // フォルダ名
      color: string          // テーマカラー
      createdAt: Date
      order: number
    }
  }
  cards: {
    key: string              // UUID
    value: {
      bookmarkId: string     // 対応するブックマークのID
      folderId: string       // 所属フォルダ
      x: number              // X座標
      y: number              // Y座標
      rotation: number       // 回転角度（度）
      scale: number          // 拡大率
      zIndex: number         // 重なり順
    }
    indexes: { 'by-folder': string }
  }
  settings: {
    key: string
    value: {
      theme: string          // 背景テーマ
      locale: string         // 表示言語
      customImage: Blob | null  // カスタム背景画像
    }
  }
}
```

---

## 9. 運用方針

Cloudflare Pages の無料枠で完全¥0運営を基本とする。
詳細な運営方針（寄付・サステナビリティ等）は非公開ドキュメントで管理。

必須公開ページ（SEO・信頼性・法令遵守用）:

| ページ | URL | 目的 |
|--------|-----|------|
| LP | `/` | SEO・コンバージョン |
| 機能紹介 | `/features` | キーワード獲得 |
| 使い方ガイド | `/guide` | ハウツー記事 |
| FAQ | `/faq` | ロングテールSEO |
| プライバシーポリシー | `/privacy` | 必須 |
| 利用規約 | `/terms` | 必須 |
| お問い合わせ | `/contact` | 運営実在性 |
| About | `/about` | 信頼性 |

---

## 10. ディレクトリ構成

```
project-root/
├── app/
│   ├── (marketing)/           # SEO用静的ページ
│   │   ├── page.tsx           # LP
│   │   ├── features/page.tsx
│   │   ├── guide/page.tsx
│   │   ├── faq/page.tsx
│   │   ├── about/page.tsx
│   │   ├── privacy/page.tsx
│   │   ├── terms/page.tsx
│   │   └── contact/page.tsx
│   ├── (app)/                 # アプリ本体
│   │   ├── board/
│   │   │   └── page.tsx       # メインキャンバス
│   │   └── layout.tsx
│   ├── api/                   # API Routes（任意・なくても動作する）
│   │   ├── oembed/route.ts    # oEmbedプロキシ（任意）
│   │   └── ogp/route.ts       # OGPスクレイパー（任意・ブックマークレット主導線のため）
│   ├── globals.css
│   └── layout.tsx
├── components/
│   ├── board/
│   │   ├── Canvas.tsx         # メインキャンバス
│   │   ├── BookmarkCard.tsx   # カード表示（全URL対応）
│   │   ├── TweetCard.tsx      # ツイート専用カード（react-tweet）
│   │   ├── DraggableCard.tsx  # ドラッグ対応ラッパー
│   │   ├── FolderNav.tsx      # フォルダ切り替えUI
│   │   ├── ExportButton.tsx   # 画像エクスポート
│   │   ├── RandomPick.tsx     # ランダムピック機能
│   │   ├── ColorSuggest.tsx   # 色別サジェスト
│   │   └── ThemeSelector.tsx  # 背景テーマ選択
│   ├── bookmarklet/
│   │   └── SavePopup.tsx      # ブックマークレットのポップアップ
│   ├── marketing/
│   │   └── LandingPage.tsx    # LP用コンポーネント
│   └── ui/                    # 共通UIコンポーネント
├── lib/
│   ├── storage/
│   │   ├── indexeddb.ts       # IndexedDB操作
│   │   └── share.ts           # URL圧縮エンコード共有
│   ├── scraper/
│   │   ├── ogp.ts             # OGPメタ情報取得
│   │   └── oembed.ts          # oEmbed取得
│   ├── utils/
│   │   ├── url.ts             # URL判定・パース
│   │   ├── color.ts           # カードの支配色抽出
│   │   └── bookmarklet.ts     # ブックマークレット生成
│   └── i18n/
│       └── config.ts          # 多言語設定
├── messages/                  # 翻訳ファイル
│   ├── ja.json
│   ├── en.json
│   └── ... (15言語)
├── public/
│   ├── manifest.json          # PWA
│   └── textures/              # 背景テクスチャ画像
├── docs/                      # 完全運営マニュアル
│   ├── 00_はじめに.md
│   ├── 01_全体像.md
│   ├── 02_日常運営.md
│   ├── 03_Cloudflare管理.md
│   ├── 04_ドメイン管理.md
│   ├── 05_コンテンツ更新.md
│   ├── 07_トラブルシューティング.md
│   ├── 08_サイト更新方法.md
│   ├── 09_緊急時対応.md
│   ├── 10_アカウント一覧.md
│   └── 11_用語集.md
├── CLAUDE.md                  # Claude Code用指示書
└── MYCOLLAGE_FULL_SPEC.md     # この文書
```

---

## 11. ロードマップ（2週間ローンチ）

### Week 1: MVP構築

| 日 | タスク | 成果物 |
|----|--------|--------|
| **Day 1** | Next.jsセットアップ + Cloudflare設定 + デザインシステム（CSS Variables, Glassmorphism, フォント, アニメーション基盤） | 骨格完成 |
| **Day 2** | OGPスクレイパーWorker + oEmbedプロキシ + IndexedDB保存 + URLペースト機能 | URL保存機能 |
| **Day 3** | コラージュキャンバス + カード表示（全URL対応） + ふわふわ浮遊アニメーション | カードが浮く |
| **Day 4** | フォルダ機能 + フォルダ切り替え散布アニメーション + ランダムピック | フォルダ動作 |
| **Day 5** | GSAPドラッグ配置 + ブックマークレット（フォルダ選択ポップアップ） | 外部保存可能 |
| **Day 6** | dom-to-image-more画像エクスポート + SNSシェアボタン + 色別サジェスト | バイラル機能 |
| **Day 7** | 背景テーマ9種 + 多言語基盤（日英） + MVP全体テスト | **MVP完成** |

### Week 2: ローンチ

| 日 | タスク | 成果物 |
|----|--------|--------|
| **Day 8** | LP（多言語対応） + デザイン磨き | 美しいLP |
| **Day 9** | Privacy, Terms, FAQ, About, Contact + SEO対策 | 公開準備 |
| **Day 10** | Remotionプロモ動画制作（TikTok/YouTube Shorts用） | 30秒動画 |
| **Day 11** | P1言語（中韓）+ P2言語（欧州5言語）追加 | 9言語対応 |
| **Day 12** | Cloudflare Pagesにデプロイ | **🚀 世界に公開** |
| **Day 13-14** | マーケティング: X, Reddit, ProductHunt, HackerNews | 初期ユーザー獲得 |

---

## 12. バイラル戦略

```
ユーザーAがコラージュ作成
  ↓
「画像として保存」ボタンでPNG出力
  ↓
「SNSにシェア」ボタン
  → "ブックマークでコラージュ作ったら最高になった✨ #Booklage"
  → 画像 + サイトURL 付きで投稿
  ↓
フォロワーBが見て「これなに！？」→ 来訪
  ↓
Bも自分のコラージュを作ってシェア
  ↓ ← これが成長エンジン
```

### ローンチ戦略
1. 完成前に開発過程をXでツイート（Build in Public）
2. ベータ版を各SNSで紹介
3. ProductHunt 同時掲載（英語圏流入）
4. Reddit r/productivity, r/webapps への投稿
5. HackerNews Show HN
6. Remotionで作ったプロモ動画をTikTok/YouTube Shortsに投稿

---

## 13. 完全運営マニュアル

開発と同時に作成する。内容:

```
docs/
├── 00_はじめに.md          ← プロジェクト概要を1ページで
├── 01_全体像.md           ← 技術構成図（図解・非エンジニア向け）
├── 02_日常運営.md          ← 日常のやることリスト
│                           （週1:アクセス確認 5 分）
├── 03_Cloudflare管理.md   ← ログイン方法・ダッシュボードの見方
│                           （スクリーンショット付き）
├── 04_ドメイン管理.md      ← ドメイン更新手順（年1回）
├── 05_コンテンツ更新.md    ← Claude Codeへの依頼テンプレート
│                           （「こう言えばClaude Codeがやる」集）
├── 07_トラブルシューティング.md ← よくある問題と対処法
├── 08_サイト更新方法.md    ← デプロイ手順（コピペで完了）
├── 09_緊急時対応.md        ← サイトダウン時の対応
├── 10_アカウント一覧.md    ← 全サービスのID/パスワード管理方法
└── 11_用語集.md           ← 技術用語の簡単な説明
```

---

## 14. プロダクトの独自性

Booklage が提供する、既存の「ブックマーク管理ツール」にない体験:

- **全URL対応のビジュアルコラージュ表現** — ブックマークを「表現ツール」として使う
- **画像エクスポート + SNS拡散前提の設計** — 作品として共有できる
- **ユーザーデータサーバー非保持** — プライバシーを最大限尊重（IndexedDB のみ）
- **多言語対応（15 言語）**
- **Moodboard 感のあるレイアウト** — 無機質な「整理ツール」ではなく「表現」に最適化

詳細な市場分析は非公開ドキュメントで管理。

---

## 付録A: 環境変数

```bash
# .env.local（ローカル開発用）
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Booklage

# Cloudflare Pages のみ使用（Workers/R2/KV は不要）
```

## 付録B: パフォーマンス目標

| 指標 | 目標 |
|------|------|
| LCP | < 2.5s |
| CLS | < 0.1 |
| First Load JS | < 200KB |
| コラージュ（50枚カード） | 60fps維持 |
| Lighthouse Score | > 90 |

---

## 15. 実装・運用ガイドライン（2026-04-10 追加）

### コア・コンセプト: No Data Collection

- 「一切のデータをサーバーに保存しない（No Data Collection）」を最大の哲学とする。
- プライバシー重視のユーザーが、コードを見て安心できる設計（オープンソース）を徹底する。
- README.md に「なぜデータを持たないのか」という想いを日英両方で記載する。

### GitHub公開と透明性の維持

- 本プロジェクトは**最初からGitHub Publicで開発**する。
- `.env` 管理を徹底し、秘密情報が混入しないよう常に監視する。
- `.gitignore` に `.env*`, `.dev.vars`, `.superpowers/` を含める。
- コードレビューでシークレット漏洩がないか毎回確認する。

### Drop-zone PiP（将来拡張: v1.2以降）

- Spotify風の浮遊窓（PiP）を実装し、他サイトからのURLドラッグ＆ドロップで保存する体験を構築する。
- **Document Picture-in-Picture API** を使用（Chromium系ブラウザ限定）。
- MVPではブックマークレットが主力。PiPはChrome限定のボーナス機能として後から追加。
- PiPの「ドラッグ＆ドロップの気持ちよさ」はプロモ動画・バイラル素材として最大限活用する。

### モバイル保存体験: Web Share Target API

- PWAとしてインストールすると、OSの共有メニューに「Booklage」が出現する。
- Twitter/YouTube/どのアプリからでも「共有→Booklage→フォルダ選択→保存」で完結。
- デスクトップChromeでもPWAインストール後は共有ボタンから保存可能。

### ブックマークレット: 「魔法の杖」チュートリアル

- ブックマークレットのセットアップは非エンジニアにとって分かりにくいため、**世界一わかりやすいチュートリアル**を用意する。
- **インタラクティブガイド**: 画面上で「ここにドラッグ！」と矢印が動く、ステップバイステップのアニメーション。
- **短い動画チュートリアル**: 15秒以内で完結する動画も併設。
- 両方を組み合わせ、ユーザーが好きな方法でセットアップできるようにする。

### 保存方法の優先順位

| 優先度 | 方法 | 対象 | タイミング |
|--------|------|------|-----------|
| **1** | ブックマークレット | デスクトップ全ブラウザ | MVP |
| **2** | Web Share Target（PWA） | モバイル全般 + デスクトップChrome | MVP |
| **3** | URLペースト | 全デバイス（フォールバック） | MVP |
| **4** | PiPドロップゾーン | Chrome/Edge限定ボーナス | v1.2 |
| **将来** | ブラウザ拡張機能 | 全デスクトップ | 将来検討 |

### グローバルSNS発信方針

- **発信スタンス**: 多言語ユーザーに向けたシンプルな発信
- **投稿スタイル**:
  1. 日本語で開発進捗を投稿
  2. 返信ツリーで、グローバルユーザー向けの英語解説を添える
- **ハッシュタグ**: #Booklage #Productivity #Privacy #NoData #BuildInPublic
- **動画コンテンツ**: 操作感を伝える動画を制作
  - Remotion で生成したプロモ動画（TikTok/YouTube Shorts 用 30 秒）
  - 実操作のスクリーンキャプチャ動画

---

*このドキュメントは承認済みの最終設計書です。*
*Claude Code は必ずこのファイルと CLAUDE.md を読んでから作業を開始すること。*
*最終更新: 2026-04-10（完全¥0設計に改訂）*
